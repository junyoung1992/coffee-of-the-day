# 빌드 컨텍스트에서 불필요한 파일을 제외해 이미지 빌드 속도를 높인다.

# Go 빌드 캐시 / 바이너리
server

# 프론트엔드 빌드 결과물과 의존성 (Dockerfile 내에서 빌드)
frontend/node_modules/
frontend/dist/
web/static/*
!web/static/.gitkeep

# 로컬 개발 데이터
data/

# Git / 문서
.git/
docs/
*.md

# 환경 설정
.env*
